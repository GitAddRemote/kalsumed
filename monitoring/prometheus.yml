global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'kalsumed-backend'
    metrics_path: /actuator/prometheus
    static_configs:
      # On macOS/Windows Docker, 'host.docker.internal' points to the host.
      - targets: ['host.docker.internal:8080']
